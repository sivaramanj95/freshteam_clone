<div class="sub-heading">
  <p class="heading-text">Employees</p>
</div>

<div class="users-list container">
  <div class="row">
    {{#each model as |user|}}
      <div class="user_column col-md-4 col-lg-3 col-xl-2">
        {{user-listing user=user}}
      </div>
    {{/each}}
  </div>  
</div>
{{outlet}}